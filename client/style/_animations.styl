/*
  Possible Animations:
  enter
  leave
  appear Must set transitionAppear={true} on animatin component
 */

.order-enter
  transition all 0.5s
  transform translateX(-120%)
  max-height 0
  padding 0 !important
  &.order-enter-active
    transform translateX(0)
    max-height 60pxpadding
    padding 2rem 0 !important

.order-leave
  transition all 0.5s
  transform translateX(0)
  &.order0leave-active
    transform translateX(120%)
    max-height 0
    padding 0